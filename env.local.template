# Neon Database Connection String
# Lấy từ Neon Dashboard > Connection Details > Connection String
# Ví dụ: postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=your_neon_database_connection_string_here

# JWT Secret Key
# Tạo một chuỗi ngẫu nhiên bảo mật
# Có thể tạo bằng: openssl rand -base64 32
# Hoặc sử dụng một chuỗi ngẫu nhiên dài và phức tạp
JWT_SECRET=your_jwt_secret_key_here

